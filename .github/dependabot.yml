version: 2
updates:
  # Keep the SHA-pinned GitHub Actions in .github/workflows current — dependabot
  # bumps the pin + its version comment when a new release ships.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
